import { AppShell } from "@/components/layout/app-shell";

export default function Page() {
  return (
    <AppShell>
      <section className="rounded-xl border border-slate-200 bg-white p-6">
        <h2 className="text-lg font-semibold text-slate-900">settings 화면 준비 중</h2>
        <p className="mt-2 text-sm text-slate-600">해당 메뉴는 다음 단계에서 순차 구현됩니다.</p>
      </section>
    </AppShell>
  );
}
